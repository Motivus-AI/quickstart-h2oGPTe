# Production: H2O AI Cloud

Runtime is **only** [Enterprise h2oGPTe on partners.h2o.ai](https://partners.h2o.ai/home).

```bash
cp .env.example .env   # H2OGPTE_API_KEY from h2oGPTe → API Keys
python src/deploy_partners.py
```

Then Chat → Agent Type `riot_helpdesk_agent`.
